{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "bash .claude/scripts/check-code-workflow.sh \"${TOOL_INPUT}\" || exit 1"
          },
          {
            "type": "command",
            "command": "bash .claude/scripts/check-file-pattern.sh \"${TOOL_INPUT}\" || { echo '❌ BLOCKED: Cannot create file versions (-v2.js, -simple.js). Modify existing files instead.'; exit 1; }"
          }
        ]
      },
      {
        "matcher": "Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "bash .claude/scripts/check-code-workflow.sh \"${TOOL_INPUT}\" || exit 1"
          },
          {
            "type": "command",
            "command": "bash .claude/scripts/verify-imports.sh \"${TOOL_INPUT}\" || { echo '⚠️  WARNING: Check that all imports exist before proceeding'; }"
          },
          {
            "type": "command",
            "command": "echo '📋 REMINDER: Comment out old code with explanation when refactoring'"
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash .claude/scripts/check-git-commit.sh \"${TOOL_INPUT}\" || { echo '🚫 BLOCKED: Git commits require explicit user permission. Ask \"Should I commit these changes?\"'; exit 1; }"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "bash .claude/scripts/verify-implementation.sh \"${TOOL_INPUT}\" || echo '⚠️  WARNING: Found incomplete function implementations'"
          },
          {
            "type": "command",
            "command": "bash .claude/scripts/check-module-size.sh \"${TOOL_INPUT}\" || echo '⚠️  WARNING: Module exceeds 200 lines - consider splitting'"
          },
          {
            "type": "command",
            "command": "bash .claude/scripts/check-theme-consistency.sh \"${TOOL_INPUT}\" || echo '⚠️  WARNING: Check theme consistency (dark glassmorphic)'"
          },
          {
            "type": "command",
            "command": "bash .claude/scripts/check-chrome-safety.sh \"${TOOL_INPUT}\" || echo '⚠️  WARNING: Chrome API calls need context validation'"
          },
          {
            "type": "command",
            "command": "echo '✅ Remember to test changes on multiple websites before committing'"
          }
        ]
      },
      {
        "matcher": "Read",
        "hooks": [
          {
            "type": "command",
            "command": "echo '📖 Reading file - Remember to understand existing code before making changes'"
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "echo '🏁 Session Complete - Checklist:'; echo '  ✓ All functions have implementations?'; echo '  ✓ Imports verified?'; echo '  ✓ Error handling added?'; echo '  ✓ Theme consistency maintained?'; echo '  ✓ Tested with user?'; echo '  ✓ No uncommitted changes without permission?'"
          }
        ]
      }
    ]
  }
}